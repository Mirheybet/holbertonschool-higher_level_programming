#!/usr/bin/python3
def print_list_integer(my_list=[]):
    if (mylist == []):
        print("".format())
    for i in my_list:
        if (int(i) is True):
            print("{}".format(i), end="\n")
